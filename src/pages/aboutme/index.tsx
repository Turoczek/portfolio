import { PageRow } from "@/components";
import { InProgress } from "@/components/Error/InProgress/InProgress";
import React from "react";

const Main = () => {
  return (
    <PageRow>
      <InProgress />
    </PageRow>
  );
};
export default Main;
