import React from "react";
import { PageRow, ErrorNotFround } from "@/components";

const NotFoundPage = () => {
  return (
    <PageRow>
      <ErrorNotFround />
    </PageRow>
  );
};

export default NotFoundPage;
